services:
  - type: web
    name: tunnel-server
    env: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server.py
    envVars:
      - key: PORT
        sync: false
      - key: RENDER
        value: "true"
